const SectionHeader = ({secHead}) => {
    return ( 
        <div className="section-header">
            <h1 id="portfolio">
                {secHead}
            </h1>
        </div>
     );
}
 
export default SectionHeader;